services:
  caddy:
    image: caddy:2-alpine
    container_name: duitlater-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - duitlater_web
    depends_on:
      - frontend
      - app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: duitlater-frontend
    restart: unless-stopped
    env_file:
      - ./frontend/.env.prod
    networks:
      - duitlater_web
    expose:
      - "3000"

  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: duitlater-app
    restart: unless-stopped
    env_file:
      - ./backend/.env.prod
    networks:
      - duitlater_web
    expose:
      - "4000"
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: duitlater-postgres
    restart: unless-stopped
    env_file:
      - ./backend/.env.prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-duitlater}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-duitlater}
    volumes:
      - duitlater_postgres_data:/var/lib/postgresql/data
    networks:
      - duitlater_web
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-duitlater} -d ${POSTGRES_DB:-duitlater}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  duitlater_postgres_data:
  caddy_data:
  caddy_config:

networks:
  duitlater_web:
    driver: bridge
