/**
 * Kutu Digitizer · Drizzle schema.
 * Phase 0: intentionally empty — tables are forged in Phase 1 (auth + tabung).
 * Module must export at least one value so drizzle-kit recognizes it.
 */
export const __schemaReady = true;
