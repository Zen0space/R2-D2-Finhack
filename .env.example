# Copy to .env (root) — only read by docker-compose for the postgres container.
# packages/db has its own .env that Prisma reads.

POSTGRES_USER=kutu
POSTGRES_PASSWORD=kutu_dev
POSTGRES_DB=kutu_digitizer
POSTGRES_PORT=5432
