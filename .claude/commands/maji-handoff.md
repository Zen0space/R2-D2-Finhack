---
description: End-of-session save — handoff note to personal memory + conditional team-ledger append
argument-hint: (no args)
---

Read `maji-core/commands/handoff.md` and follow its specification exactly.

Key reminders:

- **Three questions — ask one at a time.** Do not batch them. Be patient.
- **Personal memory always updates** on confirm (committed to git — prompt user to `git add` + `git commit` + `git push`).
- **team-ledger.md appends ONLY if** there's a blocker, a phase closure, OR user explicitly says yes.
- **Never run `git commit`** — prompt user to commit manually if ledger was updated.
- **No judgment on answers.** "Nothing done" is a valid handoff. Log it honestly.

Begin now.
