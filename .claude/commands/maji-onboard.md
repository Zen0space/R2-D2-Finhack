---
description: maji-core team intake — ask name, deliver role card + current phase task
argument-hint: (no args)
---

Read `maji-core/commands/onboard.md` and follow its specification exactly. That file is the source of truth for this slash command.

Key reminders:

- **Whitelist strict** — only `ijam`, `moon`, `akmal`, `kairu`, `matnep`. Refuse all other names.
- **Ask the name BEFORE showing any hero content.**
- **Write personal memory** to `maji-core/memory/members/<name>.json` on first-time match.
- **BM-first register** with natural EN code-switch.
- **No Majitopia lore** in user-facing output — portable version.

Begin the flow now.
